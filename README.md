# ThreeTierTrips

Upload these four files to the root of your GitHub repository:

- index.html
- style.css
- script.js
- README.md

Then enable GitHub Pages:
Repository → Settings → Pages → Deploy from branch → main → / (root) → Save.

The website is a static HTML/CSS/JS site, so no npm or build step is required.
